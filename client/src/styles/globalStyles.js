import { StyleSheet } from 'react-native';

const globalStyles = StyleSheet.create({
    h1: {
        fontSize: 24,
        fontWeight: "bold",
        marginBottom: 10,
    },
    h2: {
        fontSize: 22,
        fontWeight: "bold",
        marginBottom: 8,
    },
    h3: {
        fontSize: 20,
        fontWeight: "bold",
        marginBottom: 6,
    },
    h4: {
        fontSize: 18,
        fontWeight: "bold",
        marginBottom: 4,
    },
    h5: {
        fontSize: 16,
        fontWeight: "bold",
        marginBottom: 2,
    },
    h6: {
        fontSize: 14,
        fontWeight: "bold",
        marginBottom: 2,
    },
});

export default globalStyles;